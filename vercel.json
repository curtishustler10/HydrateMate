{
  "framework": "create-react-app",
  "buildCommand": "npm ci --prefix client && npm run build --prefix client",
  "outputDirectory": "client/build",
  "devCommand": "npm start --prefix client",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
